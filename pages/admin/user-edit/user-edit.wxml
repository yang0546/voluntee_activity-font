<view class="page">
  <view class="form-card">
    <view class="form-head">
      <text class="title">编辑用户</text>
      <text class="sub">填写用户基础信息并设置角色</text>
    </view>

    <view class="form-item">
      <text class="label">姓名 *</text>
      <input class="input" placeholder="请输入姓名" value="{{form.name}}" data-field="name" bindinput="onInputChange" />
    </view>

    <view class="form-item">
      <text class="label">手机</text>
      <input class="input" type="number" placeholder="请输入手机号码" value="{{form.phone}}" data-field="phone" bindinput="onInputChange" />
    </view>

    <view class="form-item">
      <text class="label">学院</text>
      <input class="input" placeholder="请输入学院" value="{{form.college}}" data-field="college" bindinput="onInputChange" />
    </view>

    <view class="form-item">
      <text class="label">角色</text>
      <picker mode="selector" range="{{roleOptions}}" range-key="label" value="{{roleIndex}}" bindchange="onRoleChange">
        <view class="picker-view">{{roleOptions[roleIndex].label}}</view>
      </picker>
    </view>

    <view class="actions">
      <button class="primary-btn" bindtap="handleSubmit" disabled="{{loading}}">保存修改</button>
    </view>
  </view>
</view>
