<view class="container" wx:if="{{!loading}}">
  <view class="activity-card">
    <view class="header">
      <text class="title">{{activity.title}}</text>
      <text class="status-badge status-{{activity.status}}">{{getStatusText(activity.status)}}</text>
    </view>
    <view class="section">
      <text class="label">活动说明</text>
      <text class="content">{{activity.description}}</text>
    </view>
    <view class="section">
      <text class="label">活动地点</text>
      <text class="content">📍 {{activity.location}}</text>
    </view>
    <view class="section">
      <text class="label">活动时间</text>
      <text class="content">⏰ {{formatTime(activity.startTime)}} - {{formatTime(activity.endTime)}}</text>
    </view>
    <view class="section">
      <text class="label">报名截止</text>
      <text class="content">⏳ {{formatTime(activity.signupDeadline)}}</text>
    </view>
    <view class="section">
      <text class="label">人数要求</text>
      <text class="content">👥 {{activity.currentPeople}}/{{activity.requiredPeople}}</text>
    </view>
  </view>
  <view class="actions">
    <button class="btn btn-primary" bindtap="viewSignups">报名审核</button>
    <button class="btn btn-secondary" bindtap="viewTasks">任务管理</button>
    <button class="btn btn-warning" bindtap="editActivity">编辑活动</button>
    <button class="btn btn-danger" bindtap="deleteActivity">删除活动</button>
  </view>
</view>
<view class="loading" wx:if="{{loading}}">加载中...</view>

